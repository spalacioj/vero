# Database connection settings. Replace the placeholder values for your local setup.
POSTGRES_HOST=localhost
# Keep 5432 unless another local service already uses it.
POSTGRES_PORT=5432
POSTGRES_DB=your_database_name
POSTGRES_USER=your_database_user
POSTGRES_PASSWORD=your_long_local_development_password
